import Spinner from "@/app/_components/Spinner";

function Navbar() {
  return (
    <header>
      <nav className="border w-full py-4 bg-white">Nav</nav>
    </header>
  );
}

export default Navbar;
